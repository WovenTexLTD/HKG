import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Recommended: Deploy to Vercel for full Next.js support (sitemap, contact form, etc.)
  // If deploying to a static host (Netlify, S3, GitHub Pages), uncomment the lines below:
  // output: "export",
  // trailingSlash: true,
  // images: { unoptimized: true },
};

export default nextConfig;
